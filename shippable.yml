language: ruby

rvm:
  - 1.9.3
  
env:
  - CI_REPORTS=shippable/testresults COVERAGE_REPORTS=shippable/codecoverage
  - CI_REPORTS=shippable/testresults COVERAGE_REPORTS=shippable/coverage
  
script:
  - ls -al
  - bundle show mime-types
  - exit 0

services:
  - couchdb

notifications:
  email: false

